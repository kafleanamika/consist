<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dietconsultant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DietConsultant">
        <activity android:name=".patient_chat_list"></activity>
        <activity android:name=".Chat_" />
        <activity android:name=".Consultants_for_patients" />
        <activity android:name=".Consultant_dashboard" />
        <activity android:name=".consultant_information" />
        <activity android:name=".Personal_dashboard_patients" />
        <activity android:name=".Health_Information" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>